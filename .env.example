# Appwrite Configuration
# Replace these values with your actual Appwrite project details

VITE_APPWRITE_PROJECT_ID=your_project_id_here
VITE_APPWRITE_DATABASE_ID=your_database_id_here
VITE_APPWRITE_COLLECTION_ID=your_collection_id_here
VITE_APPWRITE_BUCKET_ID=your_bucket_id_here

# How to get these values:
# 1. Go to https://cloud.appwrite.io/
# 2. Create a new project or select existing one
# 3. Copy Project ID from project settings
# 4. Create a database and copy its ID
# 5. Create a collection in the database and copy its ID
# 6. Create a storage bucket and copy its ID
# 7. Rename this file to .env and fill in your actual values